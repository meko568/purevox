# Rust / Tauri build output — huge, regenerated by `cargo build`, never belongs in git
/desktop/src-tauri/target/

# Test/sample media — not source code, don't commit video/audio test files
*.mp4
*.mkv
*.mov
*.avi
*.webm
*.mp3
*.wav

# OS/editor junk
.DS_Store
